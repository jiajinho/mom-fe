import React from 'react';
import styled from 'styled-components/macro';

import { Camera } from 'api/traffic/types';

const Wrapper = styled.div`
`;

export default (props: Camera) => {


  return (
    <div>123</div>
  )
}